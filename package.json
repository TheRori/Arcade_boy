{
  "name": "kaboomtest",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild main.js --bundle --outfile=build.js"
  },
  "private": true,
  "dependencies": {
    "build": "^0.1.4",
    "kaboom": "^3000.1.17"
  },
  "devDependencies": {
    "esbuild": "^0.23.1"
  }
}
